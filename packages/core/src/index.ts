/**
 * @sagaz/core — MCP proxy + effect ledger.
 *
 * Phase 0 scaffold: no proxy, no ledger yet. Only the package identity is exported
 * so the CLI has something to report and the build pipeline is exercised end to end.
 */

export const CORE_VERSION = "0.0.0";
